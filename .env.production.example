# Frontend compilado para Plesk.
# Copia este archivo a `.env.production` antes de ejecutar `npm run build`.
#
# Recomendado: usar subdominio para el backend Node.js en Plesk.
VITE_API_URL=https://api.lubricantes-y-repuestos-eddy.com/api
